// .concat creates and returns a new array and leaves the old ones the untouched
var array1 = ["I ", "can ", "code"];
var array2 = ["array", "functions"];

console.log(array1.concat(array2));
console.log(array1, array2)
